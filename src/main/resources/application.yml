server:
  port: 9090
spring:
  application:
    name: service-gateway
  config:
    import: optional:configserver:http://ms-config-server:8888
  cloud:
    gateway:
      default-filters:
        - name: SCGWGlobalFilter